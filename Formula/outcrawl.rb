# GENERATED by packaging/packages.sh from dist/0.2.0/manifest.json.
# Do not edit: a hand-typed sha256 is a sha256 that is one release behind.
#
# Tap layout — this file lives in the repository `outcrawlai/homebrew-tap` at
# `Formula/outcrawl.rb`, which is what makes the install line work:
#
#   brew install outcrawlai/tap/outcrawl
#
# A binary formula rather than a source build: the artefact is already
# cross-compiled and verified, so `brew install` costs one download and needs no
# Go toolchain on the customer's machine.
class Outcrawl < Formula
  desc "Browser infrastructure for agents: scrape, crawl, search and agent runs"
  homepage "https://outcrawl.ai"
  version "0.2.0"
  license "Apache-2.0"

  on_macos do
    on_arm do
      url "https://get.outcrawl.ai/cli/0.2.0/outcrawl_0.2.0_darwin_arm64.tar.gz"
      sha256 "f01454a3ceef1ed297493c18b337d66be60b43a408be2ac4b00a847cc3c0cdfc"
    end
    on_intel do
      url "https://get.outcrawl.ai/cli/0.2.0/outcrawl_0.2.0_darwin_amd64.tar.gz"
      sha256 "4be7bd4ffe5220cc59c73b788f5cde38399eae6cec8f227764abf6c2b9a6e603"
    end
  end

  on_linux do
    on_arm do
      url "https://get.outcrawl.ai/cli/0.2.0/outcrawl_0.2.0_linux_arm64.tar.gz"
      sha256 "9c5f56214c4394b0323086fabe1151f26a09e2f4dab53eb8656f8d7cf03b9870"
    end
    on_intel do
      url "https://get.outcrawl.ai/cli/0.2.0/outcrawl_0.2.0_linux_amd64.tar.gz"
      sha256 "8129eeb1d9a2cf31d809dbeb9c3a096a56127ab1ce0ab1074780ebbca3148eb4"
    end
  end

  def install
    bin.install "outcrawl"
  end

  test do
    # The version the formula claims, from the binary itself. A formula whose
    # bottle is a different release than its `version` is the failure this
    # catches, and it is one `brew test` away from being caught.
    assert_equal "0.2.0", shell_output("#{bin}/outcrawl --version").strip
    # And that the command surface loaded: the binary embeds its command tree,
    # so a build with a broken manifest exits non-zero here rather than at a
    # customer's first invocation.
    assert_match "browser infrastructure for agents", shell_output("#{bin}/outcrawl --help")
  end
end
